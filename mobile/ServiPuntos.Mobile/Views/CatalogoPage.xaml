<ContentPage ...
        Title="Catálogo">
    <StackLayout Padding="18">
        <Label Text="Selecciona estación:"/>
        <Picker ItemsSource="{Binding Ubicaciones}"
                ItemDisplayBinding="{Binding Nombre}"
                SelectedItem="{Binding UbicacionSeleccionada}"/>
        <CollectionView ItemsSource="{Binding Productos}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10"
                            Orientation="Horizontal">
                        <Label Text="{Binding Nombre}"
                                FontAttributes="Bold"
                                WidthRequest="140"/>
                        <Label Text="{Binding CostoEnPuntos, StringFormat='Puntos: {0}'}"/>
                        <Button Text="Canjear"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:CatalogoViewModel}}, Path=CanjearCommand}"
                                CommandParameter="{Binding .}"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
