<?xml version="1.0" encoding="UTF-8"?>
<Shell
    x:Class="ServiPuntos.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ServiPuntos.Mobile.Views"
    Title="ServiPuntos">

    <ShellContent
        Title="Login"
        Route="MainPage"
        ContentTemplate="{DataTemplate local:LoginPage}"/>

    <ShellContent
        Title="Saldo"
        Route="PointsPage"
        ContentTemplate="{DataTemplate local:PointsPage}"/>

    <ShellContent
        Title="Ofertas"
        Route="OffersPage"
        ContentTemplate="{DataTemplate local:OffersPage}"/>

    <ShellContent
        Title="Alertas"
        Route="AlertsPage"
        ContentTemplate="{DataTemplate local:AlertsPage}"/>

    <ShellContent
        Title="Canjear"
        Route="RedemptionPage"
        ContentTemplate="{DataTemplate local:RedemptionPage}"/>

    <ShellContent
        Title="QR Code"
        Route="QRCodePage"
        ContentTemplate="{DataTemplate local:QRCodePage}"/>

    <ShellContent
        Title="Estaciones"
        Route="LocationsPage"
        ContentTemplate="{DataTemplate local:LocationsPage}"/>

    <ShellContent
        Title="Catálogo"
        Route="CatalogPage"
        ContentTemplate="{DataTemplate local:CatalogPage}"/>

    <ShellContent
        Title="Mapa"
        Route="MapPage"
        ContentTemplate="{DataTemplate local:MapPage}"/>

    <ShellContent
        Title="Historial"
        Route="HistoryPage"
        ContentTemplate="{DataTemplate local:HistoryPage}"/>

</Shell>
